<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <ul>
	    <% for(var idx in items){ %>
	    	<li><%= items[idx].uuid+' '+items[idx].name %></li>
	    <% } %>
	</ul>
  </body>
</html>